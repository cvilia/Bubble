<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/white"
    tools:context=".activity.center.EatWahtActivity">
    <include
        android:id="@+id/actionLayout"
        layout="@layout/layout_eate_what_action_bar"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <androidx.appcompat.widget.AppCompatTextView
        android:id="@+id/foodTv"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/eat_what_today"
        android:layout_marginTop="200dp"
        android:textColor="#00723C"
        android:textSize="18sp"
        app:layout_constraintBottom_toTopOf="@id/selectBtn"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/actionLayout" />
    <androidx.appcompat.widget.AppCompatButton
        android:id="@+id/selectBtn"
        android:layout_width="180dp"
        android:layout_height="42dp"
        android:text="@string/start_select"
        android:textSize="16sp"
        android:textColor="#FDD876"
        android:background="@drawable/select_button_background"
        android:layout_marginTop="80dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/foodTv"/>

    <androidx.appcompat.widget.AppCompatImageView
        android:id="@+id/musicIv"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toBottomOf="@id/actionLayout"
        app:layout_constraintStart_toStartOf="parent"
        android:src="@drawable/music_playing"
        android:layout_marginTop="20dp"
        android:layout_marginStart="20dp"/>


</androidx.constraintlayout.widget.ConstraintLayout>